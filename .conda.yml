name: env
channels:
  - default
  - conda-forge
dependencies:
  - python=3.10
  - numpy
  - scipy
  - matplotlib
  - casadi=3.6.*
  - onnx
  - tf2onnx

  # install with:
  # conda env create --prefix .venv -f .venv.yml  